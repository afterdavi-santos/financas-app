# Copie para .env e preencha. O .env real nunca vai para o git.
POSTGRES_PASSWORD=troque-por-uma-senha-forte
# O segredo de assinatura do JWT vem de variavel de ambiente do sistema, nao
# deste arquivo. Para gerar:
#   python -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(64)).decode())"
# JWT_SECRET=...
